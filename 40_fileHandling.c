// File I/O functions in C.

// 1. fputc -- int fputc(char, FILE Pointer);
// It returns the written char. written on success.
// On failue its return EOF (end of file char.)

// 2. fputs -- int fputs( const char *s, FILE *fp);
// fputs function is used to write a null terminated string to a file in C.

// 3. fgetc -- int fgetc(FILE *fp);
// Function to read charcters from a file is fgetc.
// It returns the written char. written on success.
// On failue its return EOF (end of file char.)

// 4. fgets -- int fgets( const char *s, int n, FILE *fp);
// fputs function is used to read a null terminated string to a file in C.